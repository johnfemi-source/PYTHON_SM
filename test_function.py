from function_class import number_is_prime
import unittest import TestClass
   def test_that_number_is_prime(self):
# self.assertTrue(true)
        prime_number = 11
        is_prime = number_is_prime(11)
        self.assertTrue(is_prime)
            
